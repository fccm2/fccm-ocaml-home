gil.ml -- scm
         -- usage
      mv -- init a new version, and keep the previous version (of a file)
      new -- keep a file in the repo
      init -- start a new repo
      manifest -- produces the manifest, in .html format
      versioned -- represent the static view, (also in .html)
    
